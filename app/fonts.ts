import { Unbounded } from "next/font/google";

export const unbounded = Unbounded({
  weight: ["400", "700", "900"],
  style: ["normal"],
  subsets: ["latin"],
  display: "swap",
  variable: "--font-unbounded",
});
